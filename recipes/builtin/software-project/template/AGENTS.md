# Agent instructions

Read `NOW.md` first, then `README.md`, `ARCHITECTURE.md`, `BACKLOG.md`, and `DECISIONS.md`. Keep changes bounded, preserve cross-platform behavior, validate proportionately, and record durable decisions. Never commit secrets.

Native OMP project context lives in `.omp/AGENTS.md` and sticky rules in `.omp/RULES.md`. Keep this root file useful for Codex and other standalone discoverers.
